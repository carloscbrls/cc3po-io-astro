---
const stats = [
	{ num: '$4K', label: 'per ADA violation — lawsuits are real', source: 'DOJ ADA Enforcement' },
	{ num: '6 sec', label: 'average site load — 40% leave', source: 'Google Research' },
	{ num: '89%', label: 'of small business sites lack security headers', source: 'CC3PO Scanner Data' },
];
---

<section class="section" id="problem">
	<div class="container">
		<div class="problem-grid">
			{stats.map(s => (
				<div class="stat-card">
					<div class="stat-num">{s.num}</div>
					<div class="stat-label">{s.label}</div>
					<div class="stat-source">Source: {s.source}</div>
				</div>
			))}
		</div>
	</div>
</section>

<style>
	.problem-grid {
		display: grid;
		grid-template-columns: repeat(3, 1fr);
		gap: 32px;
	}
	.stat-card {
		padding: 40px 24px;
		border-radius: 16px;
		border: 1px solid var(--border);
		background: var(--surface);
		text-align: center;
		transition: border-color 0.3s;
	}
	.stat-card:hover {
		border-color: var(--border-hover);
	}
	.stat-num {
		font-size: clamp(2rem, 3vw, 3rem);
		font-weight: 800;
		letter-spacing: -1px;
		color: var(--text-primary);
		margin-bottom: 8px;
	}
	.stat-label {
		font-size: 14px;
		color: var(--text-secondary);
		margin-bottom: 12px;
		line-height: 1.5;
	}
	.stat-source {
		font-size: 11px;
		color: var(--text-muted);
	}
	@media (max-width: 768px) {
		.problem-grid { grid-template-columns: 1fr; }
	}
</style>
